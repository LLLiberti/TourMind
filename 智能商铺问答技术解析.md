# 智能商铺问答（RAG + 多轮对话）技术解析

## 一、功能概述

智能商铺问答是一个基于 **Spring AI** 框架的 RAG（Retrieval-Augmented Generation）应用，核心能力：

- **语义检索**：将商铺信息向量化，通过向量相似度检索最匹配用户意图的商铺
- **LLM 生成**：将检索结果作为上下文注入 Prompt，由 LLM 生成自然语言推荐
- **多轮对话**：基于 `userId:sessionId` 维度的会话管理，AI 能结合上下文理解追问
- **坐标感知**：支持传入用户坐标，按欧几里得距离对检索结果排序
- **知识库管理**：离线将 MySQL 商铺数据向量化并写入 Redis VectorStore

---

## 二、技术架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                         系统架构                                  │
│                                                                  │
│  ┌──────────┐     ┌──────────────────┐     ┌─────────────────┐  │
│  │  前端/   │ ──► │  ShopQA          │ ──► │  Conversation   │  │
│  │  API调用  │     │  Controller      │     │  Service        │  │
│  └──────────┘     └────────┬─────────┘     └────────┬────────┘  │
│                            │                        │            │
│                            ▼                        ▼            │
│                     ┌──────────────┐        ┌──────────────┐    │
│                     │  ShopQA      │        │  ChatMemory  │    │
│                     │  Service     │◄───────│  (Redis 实现) │    │
│                     │  (RAG 编排)  │        └──────────────┘    │
│                     └──────┬───────┘                            │
│                            │                                     │
│              ┌─────────────┼──────────────┐                     │
│              ▼             ▼              ▼                     │
│       ┌──────────┐  ┌──────────┐   ┌──────────┐                │
│       │  Vector  │  │  Prompt  │   │  Chat    │                │
│       │  Store   │  │ Template │   │  Client  │                │
│       │ (Redis)  │  └──────────┘   └──────────┘                │
│       └──────────┘                                              │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              知识库初始化（离线批量）                      │   │
│  │  ShopKnowledgeService                                    │   │
│  │  MySQL Shop ──► 文本拼接 ──► EmbeddingModel ──► Redis     │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 RAG 全链路数据流

```
POST /ai/shop/question { userId, sessionId?, question, userX?, userY?, limit? }
    │
    ▼
ShopQAController ── 校验 userId 非空
    │
    ▼
IConversationService.getOrCreateConversation(userId, sessionId)
    │ 返回 conversationId = "userId:sessionId"
    │
    ▼
VectorStore.similaritySearch(question, topK=limit*2, threshold=0.6)
    │ EmbeddingModel 将 question 向量化
    │ 在 Redis VectorStore 中检索最相似的 Document
    │ 从 metadata.shopId 回查 MySQL 获取完整 Shop 对象
    │ 去重 → 截取 limit 条
    │
    ▼
sortByDistance(shops, userX, userY)   // 可选：按欧几里得距离排序
    │
    ▼
buildContextFromShops(shops)          // 拼接商铺信息文本
    │  格式: 【商铺1】名称/地址/人均/评分/营业时间 ...
    │
    ▼
PromptTemplate.create({question, context})
    │
    ▼
generateAnswerWithMemory(conversationId, userPrompt, systemPrompt)
    │ ├─ chatMemory.get(conversationId)   → 获取历史消息 + 有效性校验
    │ ├─ 合并: SystemMessage + HistoryMessages + CurrentUserMessage
    │ ├─ ChatClient.prompt(allMessages).call()  → 调用 LLM
    │ └─ chatMemory.add(UserMessage) + chatMemory.add(AssistantMessage)
    │
    ▼
返回 { answer, sessionId, recommendedShops }
```

---

## 三、配置体系

### 3.1 Spring AI 核心配置

**文件**: `src/main/java/com/hmdp/config/AIConfig.java`

```java
@Configuration
public class AIConfig {
    @Bean
    public ChatClient chatClient(ChatModel chatModel) {
        return ChatClient.builder(chatModel)
                .defaultAdvisors(new SimpleLoggerAdvisor())  // 请求日志
                .build();
    }

    @Bean
    public VectorStore vectorStore(EmbeddingModel embeddingModel) {
        JedisPooled jedisPooled = new JedisPooled("127.0.0.1", 6379);
        return RedisVectorStore.builder(jedisPooled, embeddingModel).build();
    }
}
```

### 3.2 application-ai.yaml

```yaml
spring:
  ai:
    deepseek:                          # LLM: DeepSeek (兼容 OpenAI API)
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
    ollama:                            # Embedding: Ollama 本地部署
      embedding:
        options:
          model: bge-m3                # BGE-M3 模型, 1024 维向量
    vectorstore:
      redis:
        host: 127.0.0.1
        port: 6379
        index: shop-knowledge-base
        dimension: 1024
        prefix: "shop:vector:"

rag:                                   # RAG 参数
  top-k: 5
  similarity-threshold: 0.6
  max-context-shops: 10

conversation:                          # 会话参数
  timeout-minutes: 30
  max-messages: 20
  max-conversations-per-user: 5
```

### 3.3 配置类

| 配置类 | 前缀 | 参数 | 默认值 |
|---|---|---|---|
| `RagConfig` | `rag` | `topK` / `similarityThreshold` / `maxContextShops` | 5 / 0.6 / 10 |
| `ConversationConfig` | `conversation` | `timeoutMinutes` / `maxMessages` / `maxConversationsPerUser` | 30 / 20 / 5 |

---

## 四、核心模块实现

### 4.1 ChatMemory —— 基于 Redis 的聊天记忆

**文件**: `src/main/java/com/hmdp/config/ChatMemoryConfig.java`

实现 Spring AI 的 `ChatMemory` 接口，使用 Redis 三层数据结构完整管理会话。

#### Redis 数据模型

| Key 模式 | 类型 | 内容 | TTL |
|---|---|---|---|
| `conversation:meta:{userId}:{sessionId}` | Hash | conversationId, userId, createTime, lastInteractionTime, messageCount, isValid, status | timeout+10min |
| `conversation:messages:{userId}:{sessionId}` | List | 按序存储 UserMessage / AssistantMessage | 随 meta |
| `user:conversations:{userId}` | Set | 该用户的所有 sessionId | 无 |

#### 核心逻辑

**add() — 写入消息**：
```
1. 解析 conversationId → userId + sessionId
2. 消息写入 Redis List（rightPush）
3. 更新 meta Hash（messageCount++, lastInteractionTime = now）
4. 检查用户会话数是否 > 5 → 按 lastInteractionTime 删除最旧的
```

**get() — 读取历史**：
```
1. 从 meta Hash 读取会话信息
2. 检查 isValid → false 则返回空
3. 检查超时（lastInteractionTime + 30min < now）→ 标记 TIMEOUT + 返回空
4. 检查消息数 ≥ 20 → 标记 MESSAGE_LIMIT_EXCEEDED + 返回空
5. 通过检查 → 从 Redis List 读取全部消息并返回
```

**cleanupExcessConversations() — 会话数超限清理**：
```
1. 从 Set 读取用户所有 sessionId
2. 若 size ≤ maxConversationsPerUser → 跳过
3. 按 lastInteractionTime 降序排列
4. 删除排名 > maxConversationsPerUser 的会话
```

### 4.2 ConversationService —— 会话管理服务

**文件**: `src/main/java/com/hmdp/service/impl/ConversationServiceImpl.java`

封装会话的创建、校验、查询、清除逻辑，是 ChatMemory 的上层抽象。

```
getOrCreateConversation(userId, sessionId):
    ├─ sessionId 为空 → UUID.randomUUID() 去横线
    ├─ meta Key 已存在 → 复用，更新 lastInteractionTime
    └─ 不存在 → 创建 Conversation 对象，写 meta Hash，加入用户 Set
```

**会话状态枚举**：

| 状态 | 含义 | 触发条件 |
|---|---|---|
| `ACTIVE` | 正常使用中 | 默认 |
| `TIMEOUT` | 超时失效 | 30 分钟无交互 |
| `MESSAGE_LIMIT_EXCEEDED` | 消息数达上限 | messageCount ≥ 20 |
| `CLOSED` | 手动关闭 | 预留 |

### 4.3 ShopQAService —— RAG 编排核心

**文件**: `src/main/java/com/hmdp/service/impl/ShopQAServiceImpl.java`

#### 对外接口

| 方法 | 说明 |
|---|---|
| `answerShopQuestion(userId, sessionId, question, userX, userY, limit)` | 开放式商铺推荐问答 |
| `answerQuestionAboutShop(userId, sessionId, shopId, question)` | 针对指定商铺提问 |
| `clearConversation(userId, sessionId)` | 清除指定会话 |
| `getConversationInfo(userId, sessionId)` | 获取会话详情 |
| `getUserConversations(userId)` | 获取用户全部会话列表 |

#### System Prompt

```
你是一个商铺推荐助手。请根据提供的商铺信息，回答用户的问题。
如果商铺信息不足以回答问题，请说明情况并给出一般性建议。
回答要简洁、有条理，突出商铺的关键信息（评分、价格、地址等）。
支持多轮对话，请结合对话历史理解用户的追问。
```

#### User Prompt 模板

```
用户问题：{question}

相关的商铺信息：
{context}

请根据以上信息回答用户问题，如果涉及距离计算，请根据商铺地址和坐标判断。
```

#### 检索降级

当向量检索无结果时，自动切换为通用问答模式：

```java
if (relevantShops.isEmpty()) {
    return generalQuestionAnswer(conversationId, question);
    // 不注入商铺上下文，仅用 System Prompt + 用户问题
}
```

#### 距离排序

```java
// 欧几里得距离: sqrt((x1-x2)² + (y1-y2)²)
// 仅当 userX 和 userY 均非空时启用
// 排序后 Shop.distance 字段被赋值
```

### 4.4 ShopKnowledgeService —— 知识库管理

**文件**: `src/main/java/com/hmdp/service/impl/ShopKnowledgeServiceImpl.java`

负责离线将 MySQL 商铺数据向量化并写入 Redis VectorStore。

#### 知识文本构建

```
商铺名称：浪漫餐厅
商铺类型ID：1
所在区域：陆家嘴
具体地址：东方明珠对面
人均价格：300元
销量：1500
评分：5分
营业时间：10:00-22:00
地理坐标：经度121.5033，纬度31.2374
```

#### 写入流程

```
initializeShopKnowledge(shopId):
    1. shopMapper.selectById(shopId)
    2. buildShopKnowledgeText() → 拼接文本
    3. new Document(text, metadata{shopId, shopName})
    4. vectorStore.add(document) → EmbeddingModel 向量化 → 写入 Redis
    5. redisTemplate 缓存原始文本 + 元数据
```

#### 管理接口

| 接口 | 说明 |
|---|---|
| `initializeShopKnowledge(shopId)` | 初始化单个商铺 |
| `initializeAllShopKnowledge()` | 批量初始化全部商铺 |
| `updateShopKnowledge(shopId)` | 先删后增，实现更新 |
| `deleteShopKnowledge(shopId)` | 删除 Redis 中的缓存 |

---

## 五、API 接口文档

### 5.1 智能商铺问答

```
POST /ai/shop/question
```

**请求体**：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `userId` | Long | 是 | 用户 ID |
| `sessionId` | String | 否 | 会话 ID，不传则自动生成 |
| `question` | String | 是 | 用户问题 |
| `userX` | Double | 否 | 用户经度 |
| `userY` | Double | 否 | 用户纬度 |
| `limit` | Integer | 否 | 返回数量，默认 5 |

**响应**：
```json
{
    "code": 200,
    "data": {
        "answer": "根据您的需求，为您推荐以下餐厅...",
        "sessionId": "123:abc123",
        "recommendedShops": [
            {
                "id": 1, "name": "浪漫餐厅", "area": "陆家嘴",
                "address": "东方明珠对面", "avgPrice": 300,
                "score": 5, "openHours": "10:00-22:00", "distance": 1.2
            }
        ]
    }
}
```

### 5.2 针对特定商铺提问

```
POST /ai/shop/{shopId}/question
```

用于用户已进入某个商铺详情页后提问，上下文限定为该商铺。

### 5.3 会话管理接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/ai/shop/conversation/{sessionId}/info?userId=123` | 获取会话详情 |
| GET | `/ai/shop/conversations?userId=123` | 获取用户所有会话 |
| DELETE | `/ai/shop/conversation/{sessionId}?userId=123` | 清除指定会话 |

### 5.4 知识库管理接口

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/ai/shop/knowledge/init/{shopId}` | 初始化单个商铺知识库 |
| POST | `/ai/shop/knowledge/init-all` | 初始化全部商铺知识库 |

### 5.5 多轮对话示例

```
第 1 轮:  { "userId": 1, "question": "附近适合约会的餐厅？" }
         → 返回 5 家餐厅, sessionId = "1:abc"

第 2 轮:  { "userId": 1, "sessionId": "abc", "question": "第二家人均多少？" }
         → AI 结合第 1 轮结果理解"第二家"的含义

第 3 轮:  { "userId": 1, "sessionId": "abc", "question": "那第一家呢？" }
         → AI 继续结合历史上下文回答
```

---

## 六、会话生命周期

```
┌──────────┐
│   创建   │  首次请求（sessionId 为空或不存在）
└────┬─────┘  → getOrCreateConversation() 创建新会话
     ▼
┌──────────┐
│  活跃    │  每次请求:
│ (ACTIVE) │  • 更新 lastInteractionTime
└────┬─────┘  • 递增 messageCount
     │        • 右入 UserMessage + AssistantMessage 到 Redis List
     │
     ├── 30min 无交互 ──► TIMEOUT ──► 下次 get() 返回空历史
     ├── 消息数 ≥ 20  ──► MESSAGE_LIMIT_EXCEEDED
     └── 会话数 > 5   ──► 自动删除最旧会话
     ▼
┌──────────┐
│   失效   │  用户需传新 sessionId（或留空自动生成）
└────┬─────┘
     ▼
┌──────────┐
│  Redis   │  meta Key TTL 到期 (timeout+10min)
│  自动清除 │  消息 + 元数据一并清除
└──────────┘
```

---

## 七、边界情况处理

| 场景 | 处理方式 |
|---|---|
| `question` 为空/空白 | 返回 `Result.fail("问题不能为空")` |
| `userId` 为空 | Controller 层返回错误 |
| `shopId` 不存在 | 返回 `Result.fail("商铺不存在")` |
| 向量检索无结果 | 降级通用问答（不注入商铺上下文） |
| 会话已超时 | `get()` 返回空列表，自动开启新对话 |
| 消息数超限 | 同超时，标记后返回空历史 |
| 会话数超限 | 按 `lastInteractionTime` 清理最旧会话 |
| 无用户坐标 | 跳过距离排序，按语义相关性排序 |
| 重复商铺 | `distinct()` 去重 |

---

## 八、技术栈汇总

| 组件 | 技术选型 |
|---|---|
| AI 框架 | Spring AI |
| LLM | DeepSeek (deepseek-chat, temperature=0.7) |
| Embedding | Ollama + BGE-M3 (1024 维) |
| 向量存储 | RedisVectorStore (JedisPooled) |
| 会话持久化 | Redis Hash / List / Set |
| 数据库 | MySQL (MyBatis-Plus) |
| LLM 调用 | ChatClient (同步调用) |
| Prompt 构建 | PromptTemplate + SystemPromptTemplate |
| 聊天记忆 | UserSessionChatMemory (实现 ChatMemory 接口) |

### 设计要点

1. **RAG 全链路闭环**：知识库初始化 → 向量检索 → Prompt 注入 → LLM 生成
2. **会话三层校验**：`isValid` 标记 / 超时检测 / 消息数限制，每次 `get()` 实时检查
3. **自动降级**：无检索结果时切换通用问答，不中断用户体验
4. **多级限流**：消息数限制 + 会话数限制 + Redis TTL 兜底
5. **坐标感知**：可选传入位置，按欧几里得距离排序
6. **分布式就绪**：全部状态存 Redis，无本地内存依赖
