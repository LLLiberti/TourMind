# 架构规则

所有改动必须在完整项目上下文中兼容运行。

禁止硬编码、临时方案、破坏接口契约或仅适用于局部场景的实现。

优先复用现有能力，保持低耦合、易扩展和可维护。

对于重大改动，需要说明：

* 兼容性风险；
* 技术债影响；
* 受影响模块；
* 建议回归测试范围。

---

# 代码规则

优先复用，避免重复。

保持实现简单、清晰、可读。

避免不必要的抽象和过度设计。

能简化则简化。

---

# 增量开发规则

优先基于已有上下文继续工作。

避免重新开始分析。

避免重新扫描整个项目。

优先关注最近修改文件和受影响模块。

尽量复用已有结论。

除非明确要求，不要重新审查整个模块或项目。

---



# 文件读取规则

先搜索，再读取。

只读取必要文件和必要内容。

避免重复读取已分析文件。

避免读取整个大文件。

优先局部读取，而不是全文读取。

优先读取相关代码，而不是无关配置文件。

---

# 上下文复用规则

优先基于已有分析继续工作。

不要重新读取已经分析过的文件。

不要重新扫描已经分析过的模块。

默认采用增量分析模式。

只有用户明确要求时，才重新审查整个项目或整个模块。

---

# Git 规则

优先基于 git diff 分析。

关注最近修改内容。

除非明确要求，否则不要分析整个仓库。

---

# RAG 架构规则

保持索引、检索、重排、生成解耦。

避免在业务层写入检索逻辑。

保持已有检索契约稳定。

优先组合复用，避免重复实现。

---

# 测试规则

测试逻辑必须与代码一致。

优先使用少量用例覆盖更多场景。

避免重复测试。

优先复用测试工具和公共数据。

---

# MCP 使用规则

优先增量探索。

避免大范围重新扫描。

避免重复读取。

优先小范围、精确查询。

尽量减少工具输出。

能搜索就不要全文读取。

能局部读取就不要全量读取。

优先多次小查询，而不是一次大查询。

---

# Token 优化规则

保持上下文稳定。

优先继续已有分析，而不是重新开始。

避免生成重复内容。

避免重复解释已有结论。

除非明确要求，否则不要重新总结整个项目。

除非明确要求，否则不要重新生成完整代码。

优先修改已有代码，而不是重写整个文件。

避免输出过长说明。

优先给出最小必要修改。

---

# Filesystem MCP 规则

优先 search，而不是 read。

优先读取局部内容，而不是整个文件。

避免重复读取已经分析过的文件。

避免读取：

* README.md
* CLAUDE.md
* application.yml
* pom.xml

除非任务确实需要。

避免一次读取多个大文件。

---

# Git 分析规则

优先：

git diff

而不是：

全项目扫描。

优先：

最近修改文件

而不是：

整个模块。

---

# 输出规则

默认输出：

* 问题；
* 原因；
* 修改建议；
* 最小必要代码。

除非涉及重大架构调整，否则无需重复输出：

* 技术债分析；
* 回归风险；
* 兼容性说明。

直接给出结论即可。

避免冗长解释。

避免重复描述。

避免输出无意义总结。

优先简洁、准确和可执行。
