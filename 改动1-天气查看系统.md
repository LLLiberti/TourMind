# 改动1：新增天气查看系统

## 改动概述

新增天气查询功能，当用户询问景点/城市天气时，LLM 自动调用 `checkWeather` 工具获取 Open-Meteo 实时天气数据并返回给用户。

## 架构方案

```
用户："西湖今天天气怎么样？"
  │
  ├─ QueryRouter.classify() → KNOWLEDGE（走完整 RAG）
  ├─ RAG 检索 → 返回西湖 info（含坐标 x=120.14, y=30.23）
  ├─ LLM 看到上下文 + 系统提示（告知可用 checkWeather 工具）
  ├─ LLM 调用 checkWeather(location="西湖", lat=30.23, lon=120.14)
  ├─ WeatherServiceImpl → Open-Meteo API（免费，无需 API Key）
  │     ├─ 缓存命中 → 直接返回
  │     └─ 缓存未命中 → HTTP 调用 → 回写 Redis（TTL 30min）
  └─ LLM 将天气数据融入最终回答
```

## 技术选型

| 项目 | 选择 | 原因 |
|------|------|------|
| 天气 API | Open-Meteo | 免费、无需 API Key、支持经纬度 |
| HTTP 客户端 | RestTemplate | Spring Boot Web 自带 |
| 缓存 | Redis | 复用现有 RedisTemplate，TTL 30 分钟 |
| 地理编码 | Open-Meteo Geocoding API | 无坐标时兜底，免费 |

## 新增文件

| 文件 | 路径 | 说明 |
|------|------|------|
| IWeatherService.java | `tourmind-ai/src/main/java/com/hmdp/service/IWeatherService.java` | 天气服务接口 |
| WeatherServiceImpl.java | `tourmind-ai/src/main/java/com/hmdp/service/impl/WeatherServiceImpl.java` | 天气服务实现：Open-Meteo API + Redis 缓存 + WMO 天气码中文化 |
| WeatherTools.java | `tourmind-ai/src/main/java/com/hmdp/tool/WeatherTools.java` | `@Tool` 注解组件，暴露 `checkWeather` 给 LLM |
| WeatherServiceImplTest.java | `tourmind-ai/src/test/java/com/hmdp/WeatherServiceImplTest.java` | 单元测试：输入校验、缓存机制 |
| WeatherFAQTest.java | `tourmind-ai/src/test/java/com/hmdp/WeatherFAQTest.java` | FAQ 测试：天气码映射、路由验证、集成测试桩 |

## 修改文件

| 文件 | 改动内容 |
|------|----------|
| `AIConfig.java` | 注入 `WeatherTools` → `.defaultTools(spotTools, weatherTools)` |
| `RagConfig.java` | 默认系统提示词增加天气工具调用规则 |
| `application-ai.yaml` | `rag.system-prompt` 增加天气工具调用规则 |

## 工具接口

```java
@Tool(name = "checkWeather",
      description = "查询指定地点的实时天气情况。...")
public String checkWeather(
    @ToolParam(required = true) String location,
    @ToolParam(required = false) Double lat,
    @ToolParam(required = false) Double lon
)
```

## 天气码映射（WMO → 中文）

覆盖 24 种常见天气：晴天/少云/多云/雾/毛毛雨/小雨/中雨/大雨/小雪/中雪/大雪/阵雨/阵雪/雷暴/冰雹雷暴。

## FAQ 测试覆盖

3 个测试组，覆盖主要日常对话场景：
1. **天气码映射** — 晴/雨/雪/雾/雷暴等 24 种天气代码
2. **路由验证** — 天气查询不被误判为闲聊或退购票
3. **集成测试桩** — 景点天气/城市天气/多轮追问（需基础设施）

## 不需要改动的部分

- `QueryRouter` — 天气查询自然归类为 KNOWLEDGE
- `SpotQAServiceImpl` — 通用流程已支持工具调用
- `SpotController` — 现有 `/ai/spot/question` 接口通用
