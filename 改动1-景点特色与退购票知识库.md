# 改动点 1：景点特色、购票须知、退票条件 + 退购票知识库

## 改动概述

为 `tb_spot` 新增 3 个字段（`features`/`ticket_notice`/`refund_policy`），主知识库新增 `[景点特色]` section，购退票知识独立存入新建的退购票专项知识库。

## 架构设计

```
【主知识库 spot_knowledge_base】       【退购票知识库 ticket_refund_kb】
父文档（4个section）：                   子文档（2个，无父文档）：
  [景点简介]                               [购票须知] ← spot.ticketNotice
  [位置交通]                               [退票条件] ← spot.refundPolicy
  [开放须知]                             metadata: {spotId, spotName, category}
  [景点特色]  ← 新增
    ↓ split → 4 个 chunk
```

检索时按关键词意图路由：
- 含"购票/退票/预订…" → 双 KB 检索 + 合并去重
- 其他 → 仅主 KB 检索

## 改动清单

| 文件 | 改动 |
|------|------|
| `tourmind-common/…/entity/Spot.java` | 新增 `features`/`ticketNotice`/`refundPolicy` 字段 |
| `tourmind-core/…/db/hmdp.sql` | DDL 新增 3 列 |
| `tourmind-core/…/db/migration_v2.sql` | **新建**：ALTER TABLE + 31 个景点的数据填充 |
| `tourmind-ai/…/chunking/SpotKnowledgeEnricher.java` | `enrich()` 新增 `[景点特色]` section |
| `tourmind-ai/…/chunking/TicketRefundEnricher.java` | **新建**：构建购退票子文档（2个/景点） |
| `tourmind-ai/…/index/TicketRefundIndexer.java` | **新建**：写入 Qdrant `ticket_refund_kb` + ES `ticket_refund_chunks` |
| `tourmind-ai/…/config/RagConfig.java` | 新增 `TicketRefundConfig` 嵌套配置类 |
| `tourmind-ai/…/config/AIConfig.java` | 新增 4 个 Bean + `PromptTemplate` import |
| `tourmind-ai/…/resources/application-ai.yaml` | 新增 `rag.ticket-refund.*` 配置 |
| `tourmind-ai/…/retrieval/HybridDocumentRetriever.java` | 集成退购票 KB 检索管线：意图路由 + 双路检索 + 合并去重 |
| `tourmind-ai/…/test/…/SpotToolServiceTest.java` | 修复测试数据（价格单位从分改为元） |

## 部署步骤

1. 执行 `migration_v2.sql` 更新数据库
2. 重启应用
3. 重新索引景点（调用 `ParentChildIndexer.indexSpot()` 和 `TicketRefundIndexer.indexSpot()`）
