# 秒杀下单流程与状态流转文档

## 一、整体架构概览

```
  Client                    Service                     RocketMQ                    Consumer
    │                          │                            │                           │
    │  POST /voucher-order     │                            │                           │
    │  /seckill/{voucherId}    │                            │                           │
    │ ──────────────────────►  │                            │                           │
    │                          │  ┌─ seckillVoucher()       │                           │
    │                          │  │  校验 + 加锁             │                           │
    │                          │  └─ createVoucherOrder()   │                           │
    │                          │     │ 生成orderId           │                           │
    │                          │     │ sendMessageInTx ────►│ 半消息持久化                │
    │                          │     │                      │ executeLocalTx(Lua)        │
    │                          │     │                      │ COMMIT ──────────────────► │ 创建DB订单
    │                          │     │◄── Result ───────────│                            │ 扣减DB库存
    │  ◄── Result ──────────────│     │                      │                            │
```

## 二、详细阶段流程

### 阶段 1: 请求入口

```
VoucherOrderController
  POST /voucher-order/seckill/{id}
    │
    └─► voucherOrderService.seckillVoucher(voucherId)
```

### 阶段 2: 前置校验与分布式锁

```
seckillVoucher(voucherId)
  │
  ├─ [1] 查询DB优惠券信息: seckillVoucherService.getById(voucherId)
  │   ├─ beginTime > now → Result.fail("秒杀尚未开始")
  │   ├─ endTime < now   → Result.fail("秒杀已经结束")
  │   └─ stock < 1       → Result.fail("库存不足")    ← DB层粗粒度过滤
  │
  ├─ [2] 获取用户ID: UserContext.getUser().getId()     ← ThreadLocal
  │
  ├─ [3] 加分布式锁: redisson.getLock("lock:order:" + userId)
  │   ├─ tryLock(500ms等待, 10s持有) ─ 失败 → "获取锁失败"
  │   └─ 成功 → 继续
  │
  └─ [4] AOP代理调用: proxy.createVoucherOrder(voucherId)
        (确保 @Transactional 生效)
```

> **锁的作用**: 同一用户同一时刻只能有一个秒杀请求进入核心流程，防止并发重复下单。

### 阶段 3: 事务消息 — 半消息投递

```
createVoucherOrder(voucherId)
  │
  ├─ [1] 雪花算法生成 orderId: redisIdWorker.nextId("order")
  │
  ├─ [2] 构建消息体:
  │   SeckillMessage { userId, voucherId, orderId }
  │
  ├─ [3] 构建Spring Message:
  │   MessageBuilder.withPayload(seckillMessage).build()
  │
  └─ [4] rocketMQTemplate.sendMessageInTransaction(
            "seckill-topic", message, null)
```

### 阶段 4: 事务消息 — 本地事务执行

由 `SeckillTransactionListener.executeLocalTransaction()` 处理：

```
                    ┌──── sendMessageInTransaction ────┐
                    │  ① 发送半消息到 Broker              │
                    │     ├─ 成功 → 半消息持久化（消费者不可见）  │
                    │     └─ 失败 → 抛出异常, return fail      │
                    │                                      │
                    │  ② 同步调用:                         │
                    │  executeLocalTransaction(msg, arg)   │
                    │     │                               │
                    │     ├─ 从 msg.payload 提取          │
                    │     │  SeckillMessage                │
                    │     │                               │
                    │     ├─ 执行 Lua 脚本 (seckill.lua)   │
                    │     │                               │
                    │     └─ 返回值判断:                    │
                    │         ├─ 0 → COMMIT   ──────────────► 消息对消费者可见
                    │         ├─ 1 → ROLLBACK ──────────────► 消息丢弃
                    │         └─ 2 → ROLLBACK ──────────────► 消息丢弃
                    │                                      │
                    │  ③ 返回 TransactionSendResult        │
                    └──────────────────────────────────────┘
```

#### Lua 脚本原子操作 (seckill.lua)

```
Redis Key:
  seckill:stock:{voucherId}  — 优惠券库存
  seckill:order:{voucherId}  — 已下单用户集合 (Set)

执行步骤 (原子性由 Redis 单线程保证):
  [1] get seckill:stock:{voucherId}
      └─ ≤ 0 → return 1     (库存不足 — 不修改任何数据)
  
  [2] SISMEMBER seckill:order:{voucherId} userId
      └─ = 1 → return 2     (重复下单 — 不修改任何数据)
  
  [3] INCRBY seckill:stock:{voucherId} -1    ← 扣减Redis库存
  [4] SADD seckill:order:{voucherId} userId   ← 标记用户已下单
      └─ return 0           (成功)
```

#### Lua 脚本结果码

| 返回值 | 含义 | 事务状态 | Redis 状态 |
|:---:|------|:---:|------|
| 0 | 秒杀成功 | COMMIT | 库存-1, 用户已标记 |
| 1 | 库存不足 | ROLLBACK | 无变更 |
| 2 | 重复下单 | ROLLBACK | 无变更 |

### 阶段 5: 响应返回

```
TransactionSendResult.getLocalTransactionState()

  ├─ COMMIT_MESSAGE
  │   └─► return Result.ok(orderId)          ← 秒杀成功
  │
  ├─ ROLLBACK_MESSAGE
  │   ├─ 查询 Redis seckill:stock:{voucherId}
  │   │   └─ ≤0 → return "库存不足"
  │   ├─ 查询 Redis seckill:order:{voucherId}
  │   │   └─ 包含userId → return "不能重复下单"
  │   └─ return "秒杀失败"
  │
  └─ UNKNOW (概率极低)
      └─► return Result.ok(orderId)          ← Broker将触发回查
```

### 阶段 6: 消费者 — 创建数据库订单

`SeckillConsumer.onMessage(seckillMessage)` (`consumeMode = CONCURRENTLY`)

```
onMessage(SeckillMessage)
  │
  ├─ [1] 提取 userId, voucherId, orderId
  │
  ├─ [2] 构建 VoucherOrder 实体并入库
  │     voucherOrderService.save(voucherOrder)     ← DB INSERT
  │
  ├─ [3] 幂等性检查:
  │     SISMEMBER seckill:order:{voucherId} userId
  │     ├─ true → 该用户已由Lua脚本扣过Redis库存
  │     │         跳过DB库存扣减
  │     │         → 消息成功消费 ✓
  │     │
  │     └─ false → 异常场景：Lua已执行但Redis数据异常
  │                UPDATE tb_seckill_voucher
  │                SET stock = stock - 1
  │                WHERE voucher_id = ? AND stock > 0
  │                → 消息成功消费 ✓
  │
  └─ 任何步骤异常 → 消息消费失败
```

#### 消费者重试策略

```
第1次消费失败 ──[等待5秒]──► 第2次消费失败 ──[等待5秒]──► 第3次消费失败
                                                                  │
                                                                  ▼
                                                    消息进入死信队列 (DLQ)
                                                    %DLQ%seckill-consumer-group
```

**配置参数**:
- `ConsumeMode.CONCURRENTLY` — 并发消费，最高20线程 (`consume-thread-num-max=20`)
- `maxReconsumeTimes = 3` — 最多重试3次
- `delayLevelWhenNextConsume = 2` — 重试间隔5秒

> **为什么并发安全**: `UPDATE ... WHERE stock > 0` 在数据库层通过行锁保证原子性，防止超卖。

### 阶段 7: 死信队列 (DLQ)

`SeckillDlqConsumer.onMessage(seckillMessage)`

```
收到死信消息 (重试3次全部失败)
  │
  ├─ [1] WARN 日志记录:
  │     orderId, userId, voucherId
  │
  ├─ [2] Redis Set 记录:
  │     SADD seckill:dlq:orders {orderId}
  │
  └─ [3] Redis Hash 详情 (TTL 72h):
        HSET seckill:dlq:order:{orderId}
          orderId, userId, voucherId, enterDlqTime, reason
```

> 进入 DLQ 的消息需人工排查并补偿。详情在 Redis 中保留 72 小时。

### 阶段 8: 补偿任务 (遗留数据兜底)

`SeckillOrderCheckTask.checkFailedOrders()` — 每15分钟执行

```
@Scheduled(cron = "0 */15 * * * ?")
  │
  ├─ SMEMBERS seckill:failed:orders
  │   └─ 为空 → 直接返回
  │
  └─ 遍历每个 orderId:
      ├─ HGETALL seckill:failed:order:{orderId}
      │   └─ 为空 → 从Set中移除
      │
      ├─ 检查DB: existsById(orderId)
      │   └─ true → 订单已存在，清理Redis
      │
      └─ false → asyncSend 重新发送消息
```

> **注意**: 当前事务消息流程不再写入 `seckill:failed:orders`，此任务主要处理历史遗留数据。若事务消息的 `sendMessageInTransaction` 本身失败（网络断开等），已由 try-catch 直接返回错误给用户。

---

## 三、Broker 回查流程 (故障恢复)

当生产者执行 `sendMessageInTransaction` 的第①步（发送半消息）成功，但在第②步（`executeLocalTransaction`）执行前崩溃时：

```
                      Broker
                        │
  [半消息持久化成功]       │  [生产者崩溃，未执行 executeLocalTransaction]
                        │
            ┌───────────┴───────────┐
            │  Broker 等待超时后触发回查  │
            └───────────┬───────────┘
                        │
                        ▼
            checkLocalTransaction(Message msg)
                        │
          ┌─────────────┼─────────────┐
          │             │             │
          ▼             ▼             ▼
    [1] DB查订单   [2] Redis查标记   [3] 都不存在
    existsById    SISMEMBER
    orderId       orderKey userId
          │             │             │
          ▼             ▼             ▼
       COMMIT        COMMIT        ROLLBACK
    (消费者已入库)  (Lua已执行)    (本地事务未执行,
                                  丢弃半消息)
```

### 回查优先级

| 优先级 | 检查项 | 含义 | 结果 |
|:---:|------|------|:---:|
| 1 | DB: `tb_voucher_order` 存在 orderId | 消费者已完整处理 | COMMIT |
| 2 | Redis: `seckill:order:{voucherId}` 包含 userId | Lua已执行，待消费者入库 | COMMIT |
| 3 | 以上皆无 | 本地事务未执行 | ROLLBACK |

---

## 四、状态流转总图

```
用户请求
  │
  ▼
┌─────────────────┐
│   seckillVoucher │  ← DB校验(时间/库存) + Redisson锁(per user)
│   前置校验 + 加锁  │
└────────┬────────┘
         │ 校验通过
         ▼
┌─────────────────┐
│ createVoucherOrder│  ← 雪花ID → 构建消息 → sendMessageInTransaction
│   生成订单ID       │
└────────┬────────┘
         │
    ┌────┴──── 事务消息 ────┐
    │                       │
    ▼                       ▼
┌──────────┐          ┌──────────┐
│ 半消息发送  │  失败     │  抛异常   │ ──► Result.fail("系统繁忙")
│  to Broker │ ──────► │  (catch)  │
└────┬─────┘          └──────────┘
     │ 成功
     ▼
┌───────────────────┐
│ executeLocalTx    │  ← 执行 seckill.lua (原子)
│ SeckillTransaction│
│ Listener          │
└───┬───────┬───────┘
    │       │
    │       └── ROLLBACK (Lua返回1/2) ──► 消息丢弃 ──► Result.fail("库存不足"/"不能重复下单")
    │
    └──── COMMIT (Lua返回0) ──► 消息投递
                                    │
                                    ▼
                          ┌───────────────────┐
                          │  SeckillConsumer   │  ← CONCURRENTLY, maxReconsume=3
                          │  并发消费           │
                          └────────┬──────────┘
                                   │
                       ┌───────────┼───────────┐
                       │           │           │
                       ▼           ▼           ▼
                   消费成功    消费失败(重试)  重试3次后仍失败
                       │           │           │
                       │     ┌─────┘           ▼
                       │     │          ┌──────────────┐
                       │     │          │ SeckillDlq    │
                       │     │          │ Consumer      │
                       │     │          │ 记录Redis     │
                       │     │          │ 人工补偿       │
                       │     │          └──────────────┘
                       │     │
                       ▼     ▼
                  ┌──────────────┐
                  │ 1. INSERT订单 │
                  │ 2. 幂等性检查  │
                  │ 3. UPDATE库存 │
                  │    (条件更新)  │
                  └──────────────┘
                         │
                         ▼
                      秒杀完成

          ┌─────────────────────────┐
          │  SeckillOrderCheckTask   │  ← 每15分钟
          │  补偿任务 (历史数据兜底)   │
          │  扫描 failed:orders      │
          │  重发丢失的消息            │
          └─────────────────────────┘
```

---

## 五、关键 Redis Key 一览

| Key | 类型 | 用途 | 生产者 | 消费者 |
|------|:---:|------|:---:|:---:|
| `seckill:stock:{voucherId}` | String | 秒杀库存计数 | Lua `incrby -1` | — |
| `seckill:order:{voucherId}` | Set | 已下单用户集合 | Lua `sadd userId` | `sismember` 幂等性校验 |
| `lock:order:{userId}` | Redisson Lock | 用户级分布式锁 | `seckillVoucher` 加锁/解锁 | — |
| `seckill:failed:orders` | Set | 失败订单ID集合 | 旧 asyncSend 回调 | `SeckillOrderCheckTask` 扫描 |
| `seckill:failed:order:{orderId}` | Hash | 失败订单详情 | 旧 asyncSend 回调 | `SeckillOrderCheckTask` 读取 |
| `seckill:dlq:orders` | Set | DLQ 订单ID集合 | — | `SeckillDlqConsumer` 写入 |
| `seckill:dlq:order:{orderId}` | Hash | DLQ 订单详情 | — | `SeckillDlqConsumer` 写入 |

---

## 六、数据库表

### tb_voucher_order (订单表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 (雪花ID) |
| user_id | BIGINT | 用户ID |
| voucher_id | BIGINT | 优惠券ID |
| pay_type | INT | 支付方式 |
| status | INT | 订单状态: 1未支付/2已支付/3已核销/4已取消/5退款中/6已退款 |
| create_time | DATETIME | 下单时间 |
| pay_time | DATETIME | 支付时间 |

### tb_seckill_voucher (秒杀优惠券表)

| 字段 | 类型 | 说明 |
|------|------|------|
| voucher_id | BIGINT | 优惠券ID |
| stock | INT | 秒杀库存 |
| begin_time | DATETIME | 秒杀开始时间 |
| end_time | DATETIME | 秒杀结束时间 |

---

## 七、并发安全与一致性保障

| 层面 | 机制 | 保护内容 |
|------|------|------|
| 用户维度 | Redisson 分布式锁 `lock:order:{userId}` | 同一用户不能并发抢购 |
| 库存扣减 | Lua 脚本 Redis 原子操作 | 库存扣减与用户标记一步完成 |
| 消息可靠性 | RocketMQ 事务消息 (半消息+回查) | 本地事务与消息投递原子性 |
| DB库存 | `UPDATE ... WHERE stock > 0` 行锁 | 防止消费者并发超卖 |
| 幂等性 | Redis Set `sismember` 检查 + DB条件更新 | 消息重复消费不会重复扣库存 |
| 失败兜底 | DLQ 消费者记录 + 补偿任务扫描 | 消费失败不丢失数据 |
| 消息追踪 | `enableMsgTrace=true`, `customizedTraceTopic=seckill_trace` | 全链路消息追踪 |
