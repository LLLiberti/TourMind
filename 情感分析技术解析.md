# 评论情感分析——AI 功能技术解析

## 一、功能概述

评论情感分析基于 Spring AI ChatClient，利用 LLM 对用户评论进行情感三分类（好评/中评/差评），并提供商铺维度的好评率统计。

核心能力：

- **单条分析**：对单条评论内容进行实时情感分类
- **批量分析**：对指定商铺下的所有评论批量分析并缓存结果
- **好评率统计**：按商铺维度统计好评率，支持 Redis 缓存加速
- **结果持久化**：分析结果写入 Redis，30 天有效，避免重复调用 LLM

---

## 二、技术架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────┐
│                      情感分析架构                             │
│                                                              │
│  ┌──────────┐     ┌───────────────────────┐                  │
│  │  前端/   │ ──► │  SentimentAnalysis    │                  │
│  │  API调用  │     │  Controller           │                  │
│  └──────────┘     └───────────┬───────────┘                  │
│                               │                               │
│                               ▼                               │
│                     ┌──────────────────┐                     │
│                     │  Sentiment       │                     │
│                     │  Service         │                     │
│                     └────────┬─────────┘                     │
│                              │                                │
│              ┌───────────────┼────────────────┐              │
│              │               │                │              │
│              ▼               ▼                ▼              │
│       ┌──────────┐   ┌──────────────┐  ┌──────────┐        │
│       │  MySQL   │   │  ChatClient  │  │  Redis   │        │
│       │ (Blog/   │   │  (LLM 调用)  │  │ (缓存)   │        │
│       │ Comments)│   └──────────────┘  └──────────┘        │
│       └──────────┘                                          │
│                                                              │
│  Redis Key 设计:                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ blog:sentiment:{blogId}     → "POSITIVE"/"NEGATIVE"/ │    │
│  │                                "NEUTRAL" (TTL 30d)  │    │
│  │ sentiment:rate:shop:{shopId} → 0.75 (TTL 1h)        │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
POST /ai/sentiment/analyze { content: "这家店很棒！" }
    │
    ▼
SentimentAnalysisController.analyzeSentiment()
    │ 校验 content 非空
    ▼
SentimentAnalysisService.analyze(content)
    │
    ▼
ChatClient.prompt().user(String.format(SENTIMENT_PROMPT, content)).call()
    │ Prompt: "请分析以下评论的情感倾向，返回仅包含一个词：好评、中评 或 差评
    │          评论内容：这家店很棒！"
    ▼
LLM 返回 "好评"
    │
    ▼
parseSentiment("好评") → Sentiment.POSITIVE
    │
    ▼
返回 { sentiment: "POSITIVE" }
```

---

## 三、配置体系

### 3.1 SentimentConfig

**文件**: `src/main/java/com/hmdp/config/SentimentConfig.java`

```java
@Configuration
@ConfigurationProperties(prefix = "sentiment")
public class SentimentConfig {
    private int batchSize = 100;       // 批量分析大小
    private int cacheTtlHours = 1;     // 好评率缓存有效期（小时）
}
```

### 3.2 application-ai.yaml 相关配置

```yaml
spring:
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7      # 情感分析使用较低温度，保证结果稳定

sentiment:
  batch-size: 100
  cache-ttl-hours: 1
```

---

## 四、核心实现

### 4.1 情感分析 Prompt 设计

**文件**: `src/main/java/com/hmdp/service/impl/SentimentAnalysisServiceImpl.java`

```java
private static final String SENTIMENT_PROMPT = """
    请分析以下评论的情感倾向，返回仅包含一个词：好评、中评 或 差评
    
    评论内容：%s
    """;
```

**设计要点**：
- 严格限定输出格式为三个词之一，便于程序解析
- Prompt 极简，减少 Token 消耗（每条评论仅增加 ~20 个中文字符的开销）
- 不要求 LLM 输出 JSON/Markdown，减少解析复杂度

### 4.2 情感分类枚举

```java
public enum Sentiment {
    POSITIVE,   // 好评
    NEUTRAL,    // 中评
    NEGATIVE    // 差评
}
```

### 4.3 核心方法

#### 4.3.1 单条分析 —— analyze()

```java
public Sentiment analyze(String content) {
    if (content == null || content.isBlank()) {
        return Sentiment.NEUTRAL;           // 空内容默认中评
    }

    String response = chatClient.prompt()
            .user(String.format(SENTIMENT_PROMPT, content))
            .call()
            .content();                     // LLM 返回 "好评" / "中评" / "差评"

    return parseSentiment(response);        // 字符串 → 枚举
}
```

#### 4.3.2 结果解析 —— parseSentiment()

```java
private Sentiment parseSentiment(String response) {
    if (response == null) {
        return Sentiment.NEUTRAL;           // 异常情况默认中评
    }
    String trimmed = response.trim();
    if (trimmed.contains("好评")) return Sentiment.POSITIVE;
    if (trimmed.contains("差评")) return Sentiment.NEGATIVE;
    return Sentiment.NEUTRAL;               // 兜底
}
```

> 使用 `contains` 而非 `equals`，容错 LLM 可能附加额外文字（如"这是一个好评"）。

#### 4.3.3 批量分析 —— analyzeBatch()

```java
public void analyzeBatch(Long shopId) {
    // 1. 从 MySQL 查询该商铺的所有博客
    List<Blog> blogs = blogMapper.selectList(
            new LambdaQueryWrapper<Blog>().eq(Blog::getShopId, shopId)
    );

    // 2. 逐条分析并写入 Redis
    for (Blog blog : blogs) {
        Sentiment sentiment = analyze(blog.getContent());
        updateBlogSentiment(blog.getId(), sentiment);
        // Key: blog:sentiment:{blogId}, Value: "POSITIVE", TTL: 30 天
    }

    // 3. 清除好评率缓存，触发重新计算
    redisTemplate.delete(RATE_KEY_PREFIX + shopId);
}
```

**执行流程**：
```
analyzeBatch(shopId)
    │
    ├─ MySQL: SELECT * FROM blog WHERE shop_id = ?
    │
    ├─ for each blog:
    │     ├─ LLM 分析 → Sentiment 枚举
    │     └─ Redis SET blog:sentiment:{id} = "POSITIVE" EX 30d
    │
    └─ Redis DEL sentiment:rate:shop:{shopId}  (清除旧缓存)
```

#### 4.3.4 好评率统计 —— getShopPositiveRate()

```java
public double getShopPositiveRate(Long shopId) {
    // 1. 查 Redis 缓存
    Double cached = redisTemplate.opsForValue().get("sentiment:rate:shop:" + shopId);
    if (cached != null) return cached;

    // 2. 查 MySQL 获取所有博客
    List<Blog> blogs = blogMapper.selectList(...);

    // 3. 统计好评数
    long positiveCount = 0;
    for (Blog blog : blogs) {
        String sentiment = redisTemplate.opsForValue().get("blog:sentiment:" + blog.getId());
        if (sentiment == null) {
            // 缓存未命中 → 实时分析
            Sentiment s = analyze(blog.getContent());
            updateBlogSentiment(blog.getId(), s);
            sentiment = s.name();
        }
        if ("POSITIVE".equals(sentiment)) positiveCount++;
    }

    // 4. 计算并缓存好评率
    double rate = (double) positiveCount / blogs.size();
    redisTemplate.opsForValue().set(cacheKey, rate, 1, TimeUnit.HOURS);
    return rate;
}
```

**缓存策略**：

| 缓存 Key | TTL | 内容 | 更新时机 |
|---|---|---|---|
| `blog:sentiment:{blogId}` | 30 天 | POSITIVE/NEUTRAL/NEGATIVE | 分析完成时写入 |
| `sentiment:rate:shop:{shopId}` | 1 小时 | 0.0 ~ 1.0 | 好评率首次查询 / 批量分析后清除 |

---

## 五、API 接口文档

### 5.1 单条评论情感分析

```
POST /ai/sentiment/analyze
Content-Type: application/json

{
    "content": "这家店菜品很好，服务也很棒！"
}
```

**响应**：
```json
{
    "code": 200,
    "data": {
        "sentiment": "POSITIVE"
    }
}
```

### 5.2 获取商铺好评率

```
GET /ai/sentiment/shop/{shopId}/rate
```

**响应**：
```json
{
    "code": 200,
    "data": {
        "shopId": 1,
        "positiveRate": 0.75,
        "percentage": "75.0%"
    }
}
```

### 5.3 批量分析商铺评论

```
POST /ai/sentiment/shop/{shopId}/analyze-batch
```

触发后异步完成该商铺所有 Blog 的情感分析，结果写入 Redis。

---

## 六、数据流完整示例

```
场景：商铺 ID=1，有 3 条博客评论

Step 1: 管理员调用 POST /ai/sentiment/shop/1/analyze-batch
        │
        ├─ Blog 1 "味道好极了"       → LLM → "好评" → blog:sentiment:101 = POSITIVE
        ├─ Blog 2 "一般般吧"          → LLM → "中评" → blog:sentiment:102 = NEUTRAL
        ├─ Blog 3 "太差了，不推荐"    → LLM → "差评" → blog:sentiment:103 = NEGATIVE
        └─ 删除 sentiment:rate:shop:1

Step 2: 用户查询 GET /ai/sentiment/shop/1/rate
        │
        ├─ 无缓存，遍历 3 条 Blog
        ├─ 从 Redis 读取各自 sentiment（缓存命中）
        ├─ positiveCount = 1, total = 3
        ├─ rate = 0.333...
        └─ 写入缓存: sentiment:rate:shop:1 = 0.333... (TTL 1h)

返回: { "positiveRate": 0.333..., "percentage": "33.3%" }
```

---

## 七、边界情况与容错

| 场景 | 处理方式 |
|---|---|
| `content` 为空/null | `analyze()` 直接返回 `NEUTRAL` |
| `content` 为空白字符串 | Controller 层校验，返回错误 |
| LLM 返回 null | `parseSentiment()` 兜底返回 `NEUTRAL` |
| LLM 返回非预期格式 | `contains()` 模糊匹配，未命中返回 `NEUTRAL` |
| 商铺无博客 | `getShopPositiveRate()` 返回 `0.0` |
| 博客缓存未命中 | `getShopPositiveRate()` 实时调用 `analyze()` 补充分析 |
| 批量分析中途失败 | 逐条 `try-catch`，单条失败不影响其他 |
| 好评率缓存过期 | 下次查询自动触发重新计算 |

---

## 八、性能与成本优化

### 8.1 Token 消耗控制

| 项目 | 数据 |
|---|---|
| 每条 Prompt 固定开销 | ~25 tokens（中文指令） |
| 每条评论内容平均 | ~50-200 tokens |
| LLM 返回 | ~2-5 tokens（"好评"/"中评"/"差评"） |
| 单次分析总计 | ~80-230 tokens |

### 8.2 缓存收益

| 缓存层级 | 命中时节省 |
|---|---|
| `blog:sentiment:{id}` (30 天) | 避免重复 LLM 调用，直接取缓存 |
| `sentiment:rate:shop:{id}` (1 小时) | 避免遍历该商铺全部 Blog 重新计算 |

### 8.3 批量分析优化建议

- 当前为逐条串行分析，可改为并发分析（线程池 + CompletableFuture）
- 可增加全量缓存预热：定时任务扫描高频商铺，提前分析
- 可增加指数退避重试：LLM 调用失败时自动重试

---

## 九、技术栈汇总

| 组件 | 技术选型 |
|---|---|
| AI 框架 | Spring AI |
| LLM | DeepSeek (deepseek-chat, temperature=0.7) |
| LLM 调用 | ChatClient.prompt().user().call() |
| 缓存 | Redis (String 类型) |
| 数据库 | MySQL + MyBatis-Plus |
| 数据实体 | Blog（博客/评论内容） |

### 设计要点

1. **Prompt 极简化**：固定模板 + 变量插值，减少 Token 浪费
2. **两层缓存**：blog 级缓存（30 天）+ shop 级聚合缓存（1 小时），逐级减少计算
3. **容错兜底**：LLM 返回异常时默认 `NEUTRAL`，不中断业务流程
4. **惰性计算**：好评率首次查询时才计算，而非批量分析时预计算
5. **缓存穿透保护**：即使缓存未命中，也立即调用 LLM 分析并回写缓存
